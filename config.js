// config.js

const config = {
    botName: "HenAi Bot", // Nama bot yang akan ditampilkan
    adminId: "YOUR_ADMIN_ID", // Ganti dengan ID admin yang valid
    apiKey: "YOUR_API_KEY", // Ganti dengan API Key jika diperlukan
    prefixes: ["!", "?", "$", "/"], // Daftar prefix yang didukung
};

module.exports = config;

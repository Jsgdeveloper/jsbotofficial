// config.js

const config = {
    botName: "JsBot", // Nama bot yang akan ditampilkan
    adminId: "1250940447325421663", // Ganti dengan ID admin yang valid
    apiKey: "YOUR_API_KEY", // Ganti dengan API Key jika diperlukan
    prefixes: ["!", "?", "$", "/"], // Daftar prefix yang didukung
};

module.exports = config;
